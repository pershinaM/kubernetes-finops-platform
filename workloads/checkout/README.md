# checkout workload

Placeholder for the sample `checkout` application manifests and related configuration.

The workload should expose:

- request metrics,
- trace spans,
- consistent ownership labels,
- predictable traffic profile for `k6`.

The scaffold now includes:

- `base/` deployment and service manifests,
- `local/` namespace binding for Local v1.
