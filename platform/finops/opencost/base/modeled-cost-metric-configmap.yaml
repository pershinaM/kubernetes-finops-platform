apiVersion: v1
kind: ConfigMap
metadata:
  name: finops-metric-formulas
  namespace: finops-system
data:
  cost_per_1m_requests: "(modeled_cost / request_count) * 1000000"
  notes: "Modeled cost is used in Local v1. Actual cost is introduced in AWS v2."
