apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-kubernetes-capacity
  namespace: observability
  labels:
    grafana_dashboard: "1"
data:
  capacity.json: |
    {
      "title": "Kubernetes Capacity",
      "description": "Placeholder dashboard for requests, limits, utilization, and saturation.",
      "tags": ["kubernetes-capacity", "finops"],
      "schemaVersion": 39,
      "version": 1
    }
