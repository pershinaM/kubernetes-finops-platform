apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-finops
  namespace: observability
  labels:
    grafana_dashboard: "1"
data:
  finops.json: |
    {
      "title": "FinOps",
      "description": "Placeholder dashboard for modeled cost, allocation, and cost per 1M requests.",
      "tags": ["finops", "unit-economics"],
      "schemaVersion": 39,
      "version": 1
    }
