apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout
  namespace: prod
  labels:
    app.kubernetes.io/name: checkout
    app.kubernetes.io/component: api
    finops.openai.io/product: commerce
    finops.openai.io/team: payments
    finops.openai.io/environment: prod
    finops.openai.io/component: application
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: checkout
  template:
    metadata:
      labels:
        app.kubernetes.io/name: checkout
        app.kubernetes.io/component: api
        finops.openai.io/product: commerce
        finops.openai.io/team: payments
        finops.openai.io/environment: prod
        finops.openai.io/component: application
    spec:
      containers:
        - name: checkout
          image: example.local/checkout:latest
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

